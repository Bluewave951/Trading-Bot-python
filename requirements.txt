# Phase 1: Data Pipeline (implemented now)
pyyaml>=6.0
python-dotenv>=1.0
yfinance>=0.2.40
ccxt>=4.3
redis>=5.0
pandas>=2.2
numpy>=1.26

# Phase 2: Technical Indicators
pandas-ta>=0.3.14b0

# Phase 3-4: Signals & Backtesting
# backtrader not used — see TRADING_BOT_PLAN.md's "Backtrader or custom
# backtester" choice; src/backtesting/ is a lightweight custom engine to
# keep replay logic sharing entry_signals/exit_signals directly.

# Phase 5: Notifications
python-telegram-bot>=21.0
# discord.py not used — src/notifications/discord_bot.py sends alerts via
# an incoming webhook (plain `requests`), which needs no bot process.

# Phase 6: Web Dashboard (apscheduler is actually used by src/scheduler.py, not this)
fastapi>=0.111
uvicorn[standard]>=0.30
websockets>=12.0
apscheduler>=3.10
jinja2>=3.1  # not currently used — dashboard is static HTML/JS, no server templating yet

# Testing
pytest>=8.0
pytest-asyncio>=0.23
